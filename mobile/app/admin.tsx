import { RoutePlaceholder } from '@/components/RoutePlaceholder';

export default function AdminScreen() {
  return <RoutePlaceholder title="Admin" description="Admin navigation is ready for platform-level presentation flows." nextLabel="Open control" nextHref="/manager" />;
}
