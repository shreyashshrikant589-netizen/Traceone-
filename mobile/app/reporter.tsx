import { RoutePlaceholder } from '@/components/RoutePlaceholder';

export default function ReporterScreen() {
  return <RoutePlaceholder title="Reporter" description="Reporter navigation is ready for missing-person report presentation flows." nextLabel="Open cases" nextHref="/cases" />;
}
