import type { PossibleMatch } from './possibleMatch';

export const mockPossibleMatch: PossibleMatch = {
  evidence: ['Witness report near the parking area', 'Blue shirt observed moving north', 'Timestamp overlaps with active search window'],
  observations: ['Human review has not been completed', 'Candidate details are intentionally limited'],
  timestamp: 'Today, 10:16',
};
