import type { SearchSession } from './searchSession';

export const mockSearchSession: SearchSession = {
  caseName: 'Aarohi Sharma', zoneName: 'Zone A · Community Park', priority: 'High', status: 'READY', locationStatus: 'GPS_CONNECTED', elapsedSeconds: 0,
};
