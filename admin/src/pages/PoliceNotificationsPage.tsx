import { AlertTriangle, CheckCircle2, FileText, Info, LockKeyhole, Send, ShieldCheck, X } from 'lucide-react';
import { useState } from 'react';
import SectionCard from '../components/SectionCard';
import { policeNotification } from '../data/safety';

export default function PoliceNotificationsPage() {
  const [showConfirmation, setShowConfirmation] = useState(false);
  const [sent, setSent] = useState(false);

  return <div className="space-y-6"><div><p className="text-[11px] font-semibold uppercase tracking-[0.22em] text-slate-500">Safety operations</p><h1 className="mt-2 text-3xl font-semibold text-navy">Police Notifications</h1><p className="mt-2 text-sm text-slate-500">Prepare a reviewable notification package without sending it to an external system.</p></div><div className="flex items-start gap-3 rounded-2xl border border-blue/20 bg-blue/5 p-4 text-blue"><Info size={19} className="mt-0.5 shrink-0" /><p className="text-sm leading-6"><strong>Police notification integration will be connected by the backend/integration team.</strong> This page is a frontend-only preparation and review surface.</p></div>{sent ? <div className="flex items-center gap-3 rounded-xl border border-teal/20 bg-teal/10 p-4 text-teal"><CheckCircle2 size={18} /><p className="text-sm font-semibold">Mock notification marked as sent. No external notification was delivered.</p></div> : null}<section className="grid gap-6 xl:grid-cols-[1.1fr_0.9fr]"><SectionCard title="Notification package" subtitle="Case information"><div className="space-y-5"><div className="rounded-xl border border-red-200 bg-red-50 p-4"><div className="flex items-center gap-2 text-red-700"><AlertTriangle size={17} /><span className="text-sm font-semibold">Emergency level: {policeNotification.emergencyLevel}</span></div><p className="mt-2 text-sm leading-6 text-red-800">{policeNotification.reason}</p></div><div className="grid gap-4 sm:grid-cols-2">{[['Case', policeNotification.caseId], ['Person', policeNotification.person], ['Search status', policeNotification.searchStatus], ['Contact/status', policeNotification.contactStatus]].map(([label, value]) => <div key={label} className="rounded-xl border border-slate-200 bg-slate-50 p-3"><p className="text-[11px] uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 text-sm font-semibold text-slate-900">{value}</p></div>)}</div><div><p className="text-[11px] font-semibold uppercase tracking-wide text-slate-500">Evidence summary</p><p className="mt-2 rounded-xl border border-slate-200 p-4 text-sm leading-6 text-slate-700">{policeNotification.evidenceSummary}</p></div></div></SectionCard><SectionCard title="Contact and delivery" subtitle="Placeholder integration"><div className="space-y-4"><div className="flex items-start gap-3 rounded-xl border border-dashed border-slate-300 bg-slate-50 p-4"><LockKeyhole size={18} className="mt-0.5 text-slate-400" /><div><p className="text-sm font-semibold text-slate-700">External contact unavailable</p><p className="mt-1 text-xs leading-5 text-slate-500">Police contact routing and delivery status will be provided by the backend/integration team.</p></div></div><div className="rounded-xl border border-slate-200 bg-white p-4"><div className="flex items-center gap-2"><ShieldCheck size={17} className="text-blue" /><p className="text-sm font-semibold text-slate-800">Review status</p></div><p className="mt-2 text-sm text-slate-500">Draft package ready for human review.</p></div></div></SectionCard></section><SectionCard title="Notification controls" subtitle="Mock workflow"><div className="flex flex-wrap gap-3"><button className="inline-flex items-center gap-2 rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700"><FileText size={16} /> Prepare Notification</button><button className="inline-flex items-center gap-2 rounded-lg border border-blue/20 bg-blue/5 px-4 py-2.5 text-sm font-semibold text-blue"><ShieldCheck size={16} /> Review</button><button onClick={() => setShowConfirmation(true)} className="inline-flex items-center gap-2 rounded-lg bg-navy px-4 py-2.5 text-sm font-semibold text-white"><Send size={16} /> Send Notification</button></div><p className="mt-4 text-xs text-slate-500">Sending is simulated only and does not call a police API.</p></SectionCard>{showConfirmation ? <ConfirmationModal onCancel={() => setShowConfirmation(false)} onConfirm={() => { setShowConfirmation(false); setSent(true); }} /> : null}</div>;
}

function ConfirmationModal({ onCancel, onConfirm }: { onCancel: () => void; onConfirm: () => void }) {
  return <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/40 p-5"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-amber-50 text-amber-700"><AlertTriangle size={21} /></div><button onClick={onCancel} aria-label="Close confirmation" className="rounded-lg border border-slate-200 p-2 text-slate-500"><X size={17} /></button></div><h2 className="mt-5 text-xl font-semibold text-navy">Confirm simulated send</h2><p className="mt-2 text-sm leading-6 text-slate-600">This will mark the notification as sent in the mock interface only. No police API or external contact will be used.</p><div className="mt-6 flex justify-end gap-2"><button onClick={onCancel} className="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700">Cancel</button><button onClick={onConfirm} className="rounded-lg bg-navy px-4 py-2.5 text-sm font-semibold text-white">Confirm simulated send</button></div></div></div>;
}
