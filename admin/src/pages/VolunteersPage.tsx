import { Award, BriefcaseBusiness, Clock3, Eye, Search, ShieldCheck, UserRound, X } from 'lucide-react';
import { useMemo, useState } from 'react';
import PriorityBadge from '../components/PriorityBadge';
import SectionCard from '../components/SectionCard';
import StatusBadge from '../components/StatusBadge';
import EmptyState from '../components/EmptyState';
import { volunteerTimeline, volunteers } from '../data/volunteers';
import type { VolunteerStatus } from '../data/volunteers';

const filters: Array<'All' | VolunteerStatus | 'Pending verification'> = ['All', 'Active', 'Available', 'Searching', 'Offline', 'Pending verification'];

function VerificationBadge({ status }: { status: 'Verified' | 'Pending verification' }) {
  return <span className={`inline-flex rounded-full border px-2.5 py-1 text-[11px] font-semibold ${status === 'Verified' ? 'border-teal/20 bg-teal/10 text-teal' : 'border-amber-200 bg-amber-50 text-amber-700'}`}>{status}</span>;
}

export default function VolunteersPage() {
  const [query, setQuery] = useState('');
  const [filter, setFilter] = useState<(typeof filters)[number]>('All');
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const selectedVolunteer = volunteers.find((volunteer) => volunteer.id === selectedId);
  const visibleVolunteers = useMemo(() => volunteers.filter((volunteer) => {
    const normalizedQuery = query.toLowerCase().trim();
    const matchesQuery = !normalizedQuery || volunteer.id.toLowerCase().includes(normalizedQuery) || volunteer.name.toLowerCase().includes(normalizedQuery);
    const matchesFilter = filter === 'All' || volunteer.status === filter || volunteer.verification === filter;
    return matchesQuery && matchesFilter;
  }), [filter, query]);

  return (
    <div className="space-y-6">
      <div className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-[11px] font-semibold uppercase tracking-[0.22em] text-slate-500">People operations</p><h1 className="mt-2 text-3xl font-semibold text-navy">Volunteer management</h1><p className="mt-2 text-sm text-slate-500">Review volunteer readiness, assignments, credits, and activity history.</p></div><div className="rounded-full border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600 shadow-soft">Mock records • Backend/RLS authoritative</div></div>

      <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Total volunteers', volunteers.length, 'text-navy'], ['Active now', volunteers.filter((item) => item.status !== 'Offline').length, 'text-teal'], ['Searching', volunteers.filter((item) => item.status === 'Searching').length, 'text-blue'], ['Pending verification', volunteers.filter((item) => item.verification === 'Pending verification').length, 'text-amber-700']].map(([label, value, color]) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-soft"><p className="text-sm text-slate-500">{label}</p><p className={`mt-2 text-3xl font-semibold ${color}`}>{value}</p></div>)}</section>

      <SectionCard title="Volunteer directory" subtitle="Search and filter">
        <div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><label className="flex min-h-11 flex-1 items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3 text-slate-500 lg:max-w-md"><Search size={17} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search volunteer ID or name" className="w-full bg-transparent text-sm outline-none placeholder:text-slate-400" /></label><div className="flex gap-2 overflow-x-auto pb-1">{filters.map((item) => <button key={item} onClick={() => setFilter(item)} className={`whitespace-nowrap rounded-full px-3 py-1.5 text-xs font-semibold ${filter === item ? 'bg-navy text-white' : 'border border-slate-200 bg-white text-slate-600'}`}>{item}</button>)}</div></div>
          <div className="mt-5 overflow-x-auto"><table className="min-w-[1150px] w-full text-left text-sm"><thead><tr className="border-b border-slate-200 text-slate-500">{['Volunteer ID', 'Name', 'Role', 'Verification status', 'Team', 'Active case', 'Current status', 'Credits', 'Last active', ''].map((heading) => <th key={heading} className="pb-3 pr-4 font-medium">{heading}</th>)}</tr></thead><tbody>{visibleVolunteers.map((volunteer) => <tr key={volunteer.id} className="border-b border-slate-100 last:border-0"><td className="py-4 pr-4 font-semibold text-blue">{volunteer.id}</td><td className="py-4 pr-4 font-semibold text-slate-900">{volunteer.name}</td><td className="py-4 pr-4 text-slate-600">{volunteer.role}</td><td className="py-4 pr-4"><VerificationBadge status={volunteer.verification} /></td><td className="py-4 pr-4 text-slate-600">{volunteer.team}</td><td className="py-4 pr-4">{volunteer.activeCase === 'None' ? <span className="text-slate-400">None</span> : <span className="font-medium text-blue">{volunteer.activeCase}</span>}</td><td className="py-4 pr-4"><StatusBadge status={volunteer.status} /></td><td className="py-4 pr-4 font-semibold text-slate-900">{volunteer.credits}</td><td className="py-4 pr-4 text-slate-500">{volunteer.lastActive}</td><td className="py-4"><button onClick={() => setSelectedId(volunteer.id)} aria-label={`View ${volunteer.name}`} className="inline-flex rounded-lg border border-slate-200 p-2 text-slate-600 hover:text-blue"><Eye size={16} /></button></td></tr>)}</tbody></table>{visibleVolunteers.length === 0 ? <EmptyState title="No volunteers found" detail="Try a different name, ID, or availability filter." /> : null}</div>
      </SectionCard>

      {selectedVolunteer ? <div className="fixed inset-0 z-30 flex justify-end bg-slate-950/30" onClick={() => setSelectedId(null)}><aside className="h-full w-full max-w-xl overflow-y-auto bg-white p-6 shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><p className="text-[11px] font-semibold uppercase tracking-[0.22em] text-blue">Volunteer profile</p><h2 className="mt-2 text-2xl font-semibold text-navy">{selectedVolunteer.name}</h2><p className="mt-1 text-sm text-slate-500">{selectedVolunteer.id} • {selectedVolunteer.role}</p></div><button onClick={() => setSelectedId(null)} aria-label="Close profile" className="rounded-lg border border-slate-200 p-2 text-slate-500"><X size={18} /></button></div><div className="mt-5 flex flex-wrap gap-2"><VerificationBadge status={selectedVolunteer.verification} /><StatusBadge status={selectedVolunteer.status} /></div><div className="mt-6 grid grid-cols-2 gap-3">{[['Team', selectedVolunteer.team], ['Active case', selectedVolunteer.activeCase], ['Credits', `${selectedVolunteer.credits}`], ['Last active', selectedVolunteer.lastActive], ['Joined', selectedVolunteer.joined], ['Phone', selectedVolunteer.phone]].map(([label, value]) => <div key={label} className="rounded-xl border border-slate-200 bg-slate-50 p-3"><p className="text-[11px] uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 text-sm font-semibold text-slate-900">{value}</p></div>)}</div><div className="mt-6 space-y-5"><SectionCard title="Profile" subtitle="Identity and readiness"><div className="flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-blue/10 text-blue"><UserRound size={22} /></div><div><p className="font-semibold text-slate-900">Verified field profile</p><p className="text-sm text-slate-500">Available for operational assignment.</p></div></div></SectionCard><SectionCard title="Assigned cases" subtitle="Current workload"><div className="rounded-xl border border-slate-200 p-3"><div className="flex items-center gap-2"><BriefcaseBusiness size={16} className="text-blue" /><span className="text-sm font-semibold text-slate-900">{selectedVolunteer.activeCase}</span></div><p className="mt-1 text-xs text-slate-500">Primary assignment • {selectedVolunteer.team}</p></div></SectionCard><SectionCard title="Search history and reports" subtitle="Field record"><div className="space-y-2"><p className="text-sm text-slate-600">7 search shifts completed this month</p><p className="text-sm text-slate-600">3 reports submitted • 2 verified sightings</p></div></SectionCard><SectionCard title="Credits and badges" subtitle="Recognition"><div className="flex flex-wrap gap-2"><span className="inline-flex items-center gap-1 rounded-full bg-amber-50 px-3 py-1.5 text-xs font-semibold text-amber-700"><Award size={14} /> {selectedVolunteer.credits} credits</span>{selectedVolunteer.badges.map((badge) => <span key={badge} className="rounded-full bg-teal/10 px-3 py-1.5 text-xs font-semibold text-teal">{badge}</span>)}</div></SectionCard><SectionCard title="Activity timeline" subtitle="Recent activity"><div className="space-y-4">{volunteerTimeline.map((event) => <div key={event.title} className="flex gap-3"><Clock3 size={16} className="mt-0.5 shrink-0 text-blue" /><div><p className="text-sm font-semibold text-slate-900">{event.title}</p><p className="mt-1 text-xs text-slate-500">{event.detail} • {event.time}</p></div></div>)}</div></SectionCard></div></aside></div> : null}
    </div>
  );
}
