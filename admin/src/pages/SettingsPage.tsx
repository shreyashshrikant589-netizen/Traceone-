import { Bell, Check, ChevronRight, Grid3X3, Info, LockKeyhole, Monitor, ShieldCheck, UserCircle2 } from 'lucide-react';
import { useState } from 'react';
import SectionCard from '../components/SectionCard';
import { appInfo } from '../data/notificationsSettings';

export default function SettingsPage() {
  const [emailAlerts, setEmailAlerts] = useState(true);
  const [criticalOnly, setCriticalOnly] = useState(false);
  const [compactDashboard, setCompactDashboard] = useState(false);
  const [saved, setSaved] = useState(false);
  const save = () => { setSaved(true); window.setTimeout(() => setSaved(false), 1800); };

  return <div className="space-y-6"><div><p className="text-[11px] font-semibold uppercase tracking-[0.22em] text-slate-500">Administration</p><h1 className="mt-2 text-3xl font-semibold text-navy">Settings</h1><p className="mt-2 text-sm text-slate-500">Configure the admin workspace and review integration-ready preferences.</p></div>{saved ? <div className="flex items-center gap-2 rounded-xl border border-teal/20 bg-teal/10 p-4 text-sm font-semibold text-teal"><Check size={17} /> Mock preferences saved locally for this session.</div> : null}<div className="grid gap-6 xl:grid-cols-[240px_1fr]"><nav className="h-fit rounded-2xl border border-slate-200 bg-white p-2 shadow-soft">{[['Profile', UserCircle2], ['Notification preferences', Bell], ['Dashboard preferences', Grid3X3], ['Security UI', LockKeyhole], ['Application information', Info]].map(([label, Icon]) => <button key={label as string} className="flex w-full items-center justify-between rounded-xl px-3 py-3 text-left text-sm font-medium text-slate-600 hover:bg-slate-100 hover:text-navy"><span className="flex items-center gap-3"><Icon size={16} />{label as string}</span><ChevronRight size={15} /></button>)}</nav><div className="space-y-6"><SectionCard title="Profile" subtitle="Admin identity"><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-medium text-slate-700">Display name<input defaultValue="R. Singh" className="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm outline-none" /></label><label className="text-sm font-medium text-slate-700">Role<input defaultValue="Operations Lead" className="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm outline-none" /></label><label className="text-sm font-medium text-slate-700 md:col-span-2">Email<input defaultValue="admin@traceone.example" className="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm outline-none" /></label></div></SectionCard><SectionCard title="Notification preferences" subtitle="Delivery preferences"><div className="space-y-4"><Toggle label="Email alerts" detail="Receive operational notifications in the configured channel." checked={emailAlerts} onChange={setEmailAlerts} /><Toggle label="Critical alerts only" detail="Limit notification volume to high-severity updates." checked={criticalOnly} onChange={setCriticalOnly} /></div></SectionCard><SectionCard title="Dashboard preferences" subtitle="Workspace display"><div className="space-y-4"><Toggle label="Compact dashboard density" detail="Use tighter spacing for tables and command-center panels." checked={compactDashboard} onChange={setCompactDashboard} /><div className="flex items-center justify-between rounded-xl border border-slate-200 bg-slate-50 p-4"><div><p className="text-sm font-semibold text-slate-900">Default landing view</p><p className="mt-1 text-xs text-slate-500">Choose the first page shown after sign-in.</p></div><select className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600"><option>Dashboard</option><option>Live Operations</option><option>Cases</option></select></div></div></SectionCard><SectionCard title="Security UI" subtitle="Interface only"><div className="flex items-start gap-3 rounded-xl border border-amber-200 bg-amber-50 p-4"><ShieldCheck size={18} className="mt-0.5 text-amber-700" /><div><p className="text-sm font-semibold text-amber-800">Security controls are managed by the backend.</p><p className="mt-1 text-sm leading-6 text-amber-700">Password changes, sessions, roles, permissions, MFA, and authentication policy are intentionally not implemented here.</p></div></div><button disabled className="mt-4 rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-400">Manage sessions (backend)</button></SectionCard><SectionCard title="Application information" subtitle="TraceOne admin"><div className="grid gap-3 sm:grid-cols-3">{Object.entries(appInfo).map(([label, value]) => <div key={label} className="rounded-xl border border-slate-200 bg-slate-50 p-3"><p className="text-[11px] uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 text-sm font-semibold text-slate-900">{value}</p></div>)}</div></SectionCard><div className="flex justify-end"><button onClick={save} className="rounded-lg bg-navy px-4 py-2.5 text-sm font-semibold text-white">Save preferences</button></div></div></div></div>;
}

function Toggle({ label, detail, checked, onChange }: { label: string; detail: string; checked: boolean; onChange: (value: boolean) => void }) {
  return <div className="flex items-center justify-between gap-4 rounded-xl border border-slate-200 p-4"><div><p className="text-sm font-semibold text-slate-900">{label}</p><p className="mt-1 text-xs leading-5 text-slate-500">{detail}</p></div><button type="button" role="switch" aria-checked={checked} onClick={() => onChange(!checked)} className={`relative h-6 w-11 shrink-0 rounded-full transition ${checked ? 'bg-teal' : 'bg-slate-300'}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition ${checked ? 'left-6' : 'left-1'}`} /></button></div>;
}
